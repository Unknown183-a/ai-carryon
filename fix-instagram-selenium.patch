diff --git a/.github/workflows/english-scheduler.yml b/.github/workflows/english-scheduler.yml
index d961f1b..2bd1638 100644
--- a/.github/workflows/english-scheduler.yml
+++ b/.github/workflows/english-scheduler.yml
@@ -29,6 +29,9 @@ jobs:
       - name: Install ffmpeg
         run: sudo apt-get update && sudo apt-get install -y ffmpeg
 
+      - name: Set up Chrome (for Instagram posting)
+        uses: browser-actions/setup-chrome@v1
+
       - name: Install Python dependencies
         run: pip install -r requirements.txt
 
diff --git a/agents/instagram_agent.py b/agents/instagram_agent.py
index 28b15a4..a15aa81 100644
--- a/agents/instagram_agent.py
+++ b/agents/instagram_agent.py
@@ -18,7 +18,14 @@ def post_reel(video_path, caption):
     
     options = webdriver.ChromeOptions()
     options.add_argument("--user-agent=Mozilla/5.0 (iPhone; CPU iPhone OS 16_0 like Mac OS X)")
-    # Remove headless so you can see what's happening
+    # Headless is required on CI runners (GitHub Actions has no display).
+    # Set INSTAGRAM_HEADFUL=1 locally if you want to watch it run.
+    if not os.getenv("INSTAGRAM_HEADFUL"):
+        options.add_argument("--headless=new")
+        options.add_argument("--no-sandbox")
+        options.add_argument("--disable-dev-shm-usage")
+        options.add_argument("--disable-gpu")
+        options.add_argument("--window-size=1280,1696")
     
     driver = webdriver.Chrome(service=Service(ChromeDriverManager().install()), options=options)
     
diff --git a/requirements.txt b/requirements.txt
index 9bcd0a1..798ba2c 100644
--- a/requirements.txt
+++ b/requirements.txt
@@ -20,3 +20,5 @@ google-genai
 sarvamai
 pydub
 psycopg2-binary
+selenium
+webdriver-manager
